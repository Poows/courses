// main.cpp : Этот файл содержит функцию "main". Здесь начинается и заканчивается выполнение программы.
//

#include <iostream>
#include <string>
#include <cmath>
using namespace std;

int main()
{
	float N;
	cin >> N;
	cout << N;
	/*
	float N, A, B, X, Y;
	cin >> N >> A >> B >> X >> Y;
	cout << N;
	X = (100 - X) / 100;
	Y = (100 - Y) / 100;
	if (N > A)
	{
		cout << N * X;
	}
	else if (N > B)
	{
		cout << N * Y;
	}
	else
	{
		cout << N;
	}
	*/
	return 0;
}

